/* eslint-disable */
// eslint-disable-next-line no-unused-vars

import Vue from "vue";
import $ from "jquery";
import VueRouter from 'vue-router';
import axios from 'axios';

export default {
    name: 'register',
    components: {
        VueRouter
    },
    props: ['oData'],
    created() {
       
    },
    data() {
        return {
            
        }
    },
    methods: {
        
    }
}